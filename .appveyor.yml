version: 1.0.{build}

image: Visual Studio 2019

configuration: Release
platform: x64

build:
  project: DarkId.Papyrus.DebugServer.sln
  verbosity: minimal

install:
  - cd c:\tools\vcpkg
  - git pull
  - .\bootstrap-vcpkg.bat
  - vcpkg install vcpkg install fruit:x64-windows websocketpp:x64-windows jsoncpp:x64-windows
  - vcpkg integrate install
  - cd %APPVEYOR_BUILD_FOLDER%

cache:
  - c:\tools\vcpkg\installed